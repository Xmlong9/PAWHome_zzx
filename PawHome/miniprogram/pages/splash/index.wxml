<view class="splash-container paw-route {{pageVisible ? 'is-visible' : ''}} {{pageLeaving ? 'is-leaving' : ''}}">
  <image class="splash-bg" src="{{bgUrl}}" mode="aspectFill"></image>

  <view class="skip-btn" hover-class="skip-btn-hover" bindtap="onSkip">
    跳过 {{countdown}}s
  </view>
</view>
